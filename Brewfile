# Brewfile — zerobrew compatible (formulas only)
# Install: zb bundle
# Export current: zb export -f Brewfile

# ── Shell & Terminal ──────────────────────────────────────────────────────────
brew "fish"
brew "zoxide"

# ── Editor ────────────────────────────────────────────────────────────────────
brew "helix"

# ── File Management ───────────────────────────────────────────────────────────
brew "yazi"

# ── Dev Tools ─────────────────────────────────────────────────────────────────
brew "gh"
brew "git"
brew "pre-commit"
brew "watchman"

# ── Runtime Manager ───────────────────────────────────────────────────────────
# mise replaces asdf — faster, written in Rust, same workflow
brew "mise"

# ── Languages & Runtimes ──────────────────────────────────────────────────────
brew "node"
brew "pnpm"
brew "go"
brew "uv"
brew "lua"

# ── Go Tools ──────────────────────────────────────────────────────────────────
brew "golangci-lint"

# ── Language Servers ──────────────────────────────────────────────────────────
brew "typescript-language-server"
brew "vscode-langservers-extracted"
brew "prettier"

# ── Database ──────────────────────────────────────────────────────────────────
brew "postgresql@16"

# ── Ruby build deps (needed for compiling Ruby via mise) ─────────────────────
brew "libyaml"
brew "openssl@3"

# ── CLI Utilities ─────────────────────────────────────────────────────────────
brew "1password-cli"
brew "doctl"
brew "ifstat"
brew "mas"

# ── Docker CLI ────────────────────────────────────────────────────────────────
brew "docker"

# ── Note: install manually (not formula-compatible) ──────────────────────────
# claude-code → npm install -g @anthropic-ai/claude-code
# GUI apps    → 1Password, WezTerm, Warp, Chrome, Spotify (download directly)
