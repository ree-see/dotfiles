if status is-interactive
    # Commands to run in interactive sessions can go here
    # Autostart Zellij
    # eval "$(zellij setup --generate-auto-start fish)"
end

set -gx EDITOR /run/current-system/sw/bin/hx
